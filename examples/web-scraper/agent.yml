# Example: Web Scraper Agent
# An agent that scrapes content from specific sites and saves results.
# Metadata only; Docker Compose does not read policy from this file.
# Configure resources in .env, egress in config/squid.conf, and observational
# output checks in config/validation-rules.json.

agent:
  name: web-scraper
  description: "Scrapes allowlisted websites and extracts structured data"
